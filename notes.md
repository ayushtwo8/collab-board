1. initialized an empty turborepo
2. deleted the docs app
3. added http-server and ws-server
4. added package.json in both servers
5. added tsconfig.json in both the places and imported it from @repo/typescript-config/base.json
6. added @repo/typescript-config as dependency in both the servers
7. added a build, dev and start script to both the projects
8. update the turbo-config in both projects --optional
9. initialize a http-server, websocket server